{% extends 'base.html.twig' %}

{% block title %}My homepage{% endblock %}

{% block pagename %}Please have a seat.{% endblock %}

{% block body %}
  <div class="content content-halfpage">
    <h1 class="content-heading">Welcome!</h1>
    <img src="{{ asset('images/pfp.jpg') }}" alt="My profile picture" class="float-right small bordered" />
    <p>Welcome to my page. Be sure to check out my node.js <a href="https://m3tro1d.herokuapp.com" target="_blank" class="external">website</a>.</p>

    <h3 class="content-heading">My skills and hobbies</h3>
    <ul>
      <li>Computer programming: C, C++, Python, JavaScript, a bit of Scheme, some of batch and Vim scripting;</li>
      <li>Cycling;</li>
      <li>Casual article writing.</li>
    </ul>
    
    <h3 class="content-heading">Stuff</h3>
    <ul>
      <li><a href="{{ path('homepage-movies') }}">My favorite movies</a></li>
      <li><a href="{{ path('homepage-lucky') }}">Get your lucky number!</a></li>
      <li><a href="{{ path('homepage-headers') }}">Your request headers</a></li>
    </ul>

    <h3 class="content-heading">My favorite things</h3>
    <small><i>Dynamically refreshed!</i></small>
    <div class="favorites">
      {% for header, urls in favorites %}
        <div class="favorites__block">
          <h4 class="favorites__heading">{{ header }}</h4>
          {%for url in urls %}
            <img src="{{ url }}" alt="URL" class="favorites__image" />
          {% endfor %}
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock %}
